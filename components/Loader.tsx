// TODO make a better loader, probably make different component for different places

export const Loader = () => {
  return <>Loading...</>;
};

export const RedirectLoader = () => {
  return <>Redirecting...</>;
};
